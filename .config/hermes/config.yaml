model:
  provider: "google"
  default: "gemini-3.6-flash"

agent:
  reasoning_effort: "minimal"

platform_toolsets:
  cli:
    - file
    - memory
    - terminal
    - web
  tui:
    - file
    - memory
    - terminal
    - web
